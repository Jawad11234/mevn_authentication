import { createStore } from "vuex";
import AuthModel from "../warehouse/AuthModel";

export default createStore({
  state: {},
  getters: {},
  mutations: {},
  actions: {},
  modules: {
    AuthModel,
  },
});
